import AdminLayout from "@/layouts/admin-layout";

export default function PlanIndex() {
    return <div>PlanIndex</div>;
}

PlanIndex.layout = (page: React.ReactNode) => <AdminLayout>{page}</AdminLayout>;